#include "main.h"
#include <string.h>
/**
 * _strlen - function used to check the length of a string
 * @s: parameter of char type
 *
 * Return: Always 0
 */
int _strlen(char *s)
{
	int length = 0;

	while (*s != 0)
	{
		lenth++;
		s++;
	}

	return(length);
}
